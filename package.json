{
  "name": "calculadora-calificaciones-cicd",
  "version": "1.1.0",
  "private": true,
  "description": "Prueba de concepto académica de CI/CD con GitHub Actions y GitHub Pages.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node scripts/check.js",
    "test": "node --test tests/calificaciones.test.js",
    "build": "node scripts/build.js",
    "verify": "npm run check && npm test && npm run build",
    "serve": "node scripts/serve.js",
    "start": "npm run build && npm run serve"
  },
  "license": "MIT"
}
